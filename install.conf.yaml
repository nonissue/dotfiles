- clean: ['~']

- link:
    ~/.gitconfig: gitconfig
    # Probably don't want to share this between machines...
    # ~/.ssh/config:
    #     path: ssh/config
    #     create: true
    ~/.zshrc: zshrc
    ~/.tmux.conf: tmux/tmux.conf
    ~/.tmux: tmux
    ~/.config/nvim:
        path: nvim
    ~/.config/glances: 
        path: glances
    ~/.config/fish: 
        relink: true
        path: fish
    ~/.vscode: 
        if: '[ `uname` = Darwin ]'
        path: vscode
    ~/.fdignore:
        path: fdignore


