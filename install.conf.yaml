# - verbose: true

- clean: ["~"]

- create:
      - ~/.config
      - ~/tmp
      - ~/.local
      - ~/.local/bin

- link:
      #    ~/.gitconfig: gitconfig
      ~/.gitignore_global: .gitignore_global
      # VSCode disabled since I use in app settings sync anyway?
      # ~/Library/Application Support/Code/User:
      # if: '[ (uname) = "Darwin" ]'
      # path: vscode
      # Probably don't want to share this between machines...
      # ~/.ssh/config:
      #     path: ssh/config
      #     create: true
      ~/.tmux.conf: tmux/tmux.conf
      ~/.tmux: tmux
      # make ~/.config if it doesn't exist
      ~/.config/nvim:
          path: nvim
      ~/.config/bat:
          path: bat
      ~/.config/glances:
          path: glances
      ~/.config/fish:
          relink: true
          path: fish
      ~/.config/lf/lfrc:
          create: true
          path: lf/lfrc
      ~/.vscode:
          if: '[ "$(uname)" = "Linux" ]'
          path: vscode
      ~/.fdignore:
          path: fdignore
          # make ~/.local/bin if it doesn't exist
      ~/.local/bin/pbcopy:
          # Better than running command with sudo...
          # Have to add it ~/bin to path on linux
          if: '[ "$(uname)" = "Linux" ]'
          path: bin/pbcopy
      ~/Library/Application Support/com.mitchellh.ghostty/config:
          if: '[ "$(uname)" = "Darwin" ]'
          create: true
          path: ghostty/config.ghostty

- plugins:
      - dotbot-plugins/dotbot-git/

- git:
      "~/.tmux/plugins/tpm":
          url: "https://github.com/tmux-plugins/tpm"
          description: "tmux plugins (tpm)"

- shell:
      - [git submodule update --init --recursive, Installing submodules]
      - command: scripts/install-motd.sh
        description: Linking MOTD scripts (Linux only, needs sudo)
